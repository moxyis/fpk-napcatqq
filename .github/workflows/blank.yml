name: 版本比对后自动打包napcat-docker并更新仓库

on:
  workflow_dispatch:  # 手动触发
  schedule:
    - cron: '0 0 * * *'  # 每日UTC 0点（北京时间8点）执行

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 授予写入仓库/创建Release权限
      pull-requests: read
    timeout-minutes: 15  # 超时保护（打包流程耗时较长）
    defaults:
      run:
        shell: bash
        working-directory: .
    steps:
      # ===================== 第一步：环境准备 =====================
      - name: 1. 检出当前仓库代码（带完整历史）
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 完整检出历史
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 2. 安装基础依赖（增强版）
        run: |
          sudo apt update && sudo apt install -y \
            jq curl sed skopeo coreutils findutils  # 补充基础工具
          # 验证依赖安装
          for tool in jq curl sed skopeo; do
            if ! command -v $tool &>/dev/null; then
              echo "❌ $tool 安装失败"
              exit 1
            fi
          done
          echo "✅ 所有基础依赖安装完成"

      - name: 3. 配置Git全局身份（跨仓库通用）
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global core.autocrlf false  # 避免换行符问题
          git config --global pull.rebase false     # 避免rebase冲突
          echo "✅ Git身份配置完成"

      # ===================== 第二步：版本比对（核心前置） =====================
      - name: 4. 读取本地manifest版本号（鲁棒版，修复换行符问题）
        id: local_ver
        run: |
          if [ -f "fpk-napcatqq/manifest" ]; then
            # 强制移除换行符和空格，确保无隐藏字符
            LOCAL_VERSION=$(sed -n 's/^[[:space:]]*version[[:space:]]*[:=][[:space:]]*//p' fpk-napcatqq/manifest \
              | tr -d '"' | tr -d "'" | sed 's/^v//' \
              | tr -d '\n' | xargs)
            echo "本地manifest版本：[$LOCAL_VERSION]"  # 带边界符检查
            echo "local_version=$LOCAL_VERSION" >> $GITHUB_OUTPUT
          else
            echo "❌ 未找到fpk-napcatqq/manifest，强制触发更新"
            echo "local_version=0.0.0" >> $GITHUB_OUTPUT
            mkdir -p fpk-napcatqq
            echo "version: 0.0.0" > fpk-napcatqq/manifest
            git add fpk-napcatqq/manifest
            git commit -m "chore: 初始化manifest文件" || echo "无需要提交的变更"
          fi

      # ===================== 核心逻辑：获取远程版本和哈希 =====================
      - name: 5. 从Docker Hub原始API获取最新版本号
        id: remote_ver
        run: |
          set -euo pipefail
          
          API_URL="https://registry.hub.docker.com/v2/repositories/mlikiowa/napcat-docker/tags"
          RAW_RESPONSE=$(curl -s "$API_URL")
          REMOTE_VERSION=""
          REMOTE_DIGEST=""
          TOTAL_TAGS=0
          
          echo "📡 从API提取版本信息..."
          
          # 统计总标签数
          TOTAL_TAGS=$(echo "$RAW_RESPONSE" | jq -r '.count // 0')
          
          # 提取有效标签（排除测试版，保留vX.Y.Z格式）
          ALL_VALID_TAGS=$(echo "$RAW_RESPONSE" | jq -r '.results[] | .name' \
            | grep -v -E '^latest$|^dev$|^beta$|^alpha$|^nightly$|^test$' \
            | grep -E '^v[0-9]+\.[0-9]+(\.[0-9]+)?$')
          
          # 无有效标签则终止
          if [ -z "$ALL_VALID_TAGS" ]; then
            echo "❌ 未找到有效版本标签，终止流程"
            exit 1
          fi
          
          # 版本号数字排序（补全X.Y为X.Y.0）
          SORTED_TAGS=$(echo "$ALL_VALID_TAGS" | while read -r tag; do
            ver=${tag#v}
            [[ "$ver" =~ ^[0-9]+\.[0-9]+$ ]] && ver="$ver.0"
            echo "$ver|$tag"
          done | sort -t. -k1,1n -k2,2n -k3,3n)
          
          # 取最新版本标签
          LATEST_TAG=$(echo "$SORTED_TAGS" | tail -n1 | cut -d'|' -f2)
          REMOTE_VERSION="${LATEST_TAG#v}"
          
          # 提取镜像哈希（处理隐藏字符）
          REMOTE_DIGEST=$(echo "$RAW_RESPONSE" | jq -r --arg tag "$LATEST_TAG" \
            '.results[] | select(.name == $tag) | .images[0].digest // ""' \
            | tr -d '\n' | xargs)
          
          # 哈希兜底
          if [ -z "$REMOTE_DIGEST" ] || [ "$REMOTE_DIGEST" = "null" ]; then
            REMOTE_DIGEST=$(skopeo inspect --format '{{.Digest}}' \
              docker://mlikiowa/napcat-docker:${LATEST_TAG} 2>/dev/null \
              | tr -d '\n' | xargs || echo "")
          fi
          
          echo "✅ 最新版本：[$REMOTE_VERSION]，哈希：[${REMOTE_DIGEST:0:12}...]"
          echo "remote_version=$REMOTE_VERSION" >> $GITHUB_OUTPUT
          echo "remote_digest=$REMOTE_DIGEST" >> $GITHUB_OUTPUT
          echo "total_tags=$TOTAL_TAGS" >> $GITHUB_OUTPUT

      - name: 6. 读取本地哈希记录（修复隐藏字符）
        id: local_digest
        run: |
          set -euo pipefail
          DIGEST_FILE="docker-image-digest.txt"
          
          [ ! -f "$DIGEST_FILE" ] && touch "$DIGEST_FILE"
          LOCAL_DIGEST=$(cat "$DIGEST_FILE" | tr -d '\n' | xargs || echo "")
          echo "本地哈希：[${LOCAL_DIGEST:0:12}...]"
          echo "local_digest=$LOCAL_DIGEST" >> $GITHUB_OUTPUT

      - name: 7. 版本+哈希比对（精确判断）
        id: compare
        run: |
          set -euo pipefail
          LOCAL_VER="${{ steps.local_ver.outputs.local_version }}"
          REMOTE_VER="${{ steps.remote_ver.outputs.remote_version }}"
          LOCAL_DIG="${{ steps.local_digest.outputs.local_digest }}"
          REMOTE_DIG="${{ steps.remote_ver.outputs.remote_digest }}"
          
          echo -e "\n🔍 原始值比对（带边界符）："
          echo "LOCAL_VER: [$LOCAL_VER]"
          echo "REMOTE_VER: [$REMOTE_VER]"
          echo "LOCAL_DIG: [$LOCAL_DIG]"
          echo "REMOTE_DIG: [$REMOTE_DIG]"
          
          # 精确判断逻辑
          if [ "$LOCAL_VER" != "$REMOTE_VER" ] || { [ "$LOCAL_VER" = "$REMOTE_VER" ] && [ "$LOCAL_DIG" != "$REMOTE_DIG" ] && [ -n "$LOCAL_DIG" ] && [ -n "$REMOTE_DIG" ]; }; then
            echo "✅ 需要更新（版本或哈希不一致）"
            echo "need_update=true" >> $GITHUB_OUTPUT
          else
            echo "✅ 无需更新（版本和哈希完全一致）"
            echo "need_update=false" >> $GITHUB_OUTPUT
          fi

      # ===================== 第三步：仅需更新时执行 =====================
      - name: 8. 更新manifest+哈希记录
        if: steps.compare.outputs.need_update == 'true'
        run: |
          set -euo pipefail
          MANIFEST_PATH="fpk-napcatqq/manifest"
          DIGEST_FILE="docker-image-digest.txt"
          NEW_VER="${{ steps.remote_ver.outputs.remote_version }}"
          NEW_DIG="${{ steps.remote_ver.outputs.remote_digest }}"
          
          # 更新版本号（避免多余换行）
          sed -i -E "s/(^[[:space:]]*version[[:space:]]*[:=][[:space:]]*)([\"']?).*([\"']?)/\1\2${NEW_VER}\3/" "$MANIFEST_PATH"
          
          # 更新哈希（echo -n 避免换行）
          echo -n "$NEW_DIG" > "$DIGEST_FILE"
          
          # 验证更新
          echo -e "\n✅ 更新结果验证："
          echo "manifest版本: [$(grep -i version "$MANIFEST_PATH" | sed 's/.*=//' | tr -d ' "' | xargs)]"
          echo "哈希记录: [$(cat "$DIGEST_FILE")]"
          git add "$MANIFEST_PATH" "$DIGEST_FILE"

      - name: 9. 提交并推送变更
        if: steps.compare.outputs.need_update == 'true'
        run: |
          set -euo pipefail
          COMMIT_MSG="chore: 更新至版本${{ steps.remote_ver.outputs.remote_version }}"
          
          git commit -m "$COMMIT_MSG" || echo "✅ 无实际变更需提交"
          git push origin HEAD:${{ github.ref_name }} || (
            git pull --rebase origin ${{ github.ref_name }} && git push origin HEAD:${{ github.ref_name }}
          )
          echo "✅ 变更已推送至仓库"

      - name: 10. 下载并安装fnpack
        if: steps.compare.outputs.need_update == 'true'
        run: |
          curl -SL --retry 3 --connect-timeout 10 -o /usr/local/bin/fnpack \
            "https://static2.fnnas.com/fnpack/fnpack-1.0.4-linux-amd64"
          chmod +x /usr/local/bin/fnpack
          echo "✅ fnpack安装完成：$(fnpack --version 2>&1 | head -n1)"

      - name: 11. 执行fnpack打包
        if: steps.compare.outputs.need_update == 'true'
        id: build_fpk
        run: |
          set -euo pipefail
          cd fpk-napcatqq
          fnpack build
          
          FPK_FILE=$(find . -name "*.fpk" -type f | head -n1)
          [ -z "$FPK_FILE" ] && { echo "❌ 未生成fpk文件"; exit 1; }
          
          VERSION="${{ steps.remote_ver.outputs.remote_version }}"
          ARCH="Linux-AMD64"
          FPK_VERSION_NAME="fpk-napcatqq-v${VERSION}-${ARCH}.fpk"
          mkdir -p /tmp/fpk_build
          cp "$FPK_FILE" "/tmp/fpk_build/$FPK_VERSION_NAME"
          
          echo "✅ 打包完成：/tmp/fpk_build/$FPK_VERSION_NAME"
          echo "fpk_release_file=/tmp/fpk_build/$FPK_VERSION_NAME" >> $GITHUB_OUTPUT
          echo "fpk_release_name=$FPK_VERSION_NAME" >> $GITHUB_OUTPUT

      # ===================== 第四步：推送至FnDepot仓库 =====================
      - name: 12. 克隆FnDepot仓库
        if: steps.compare.outputs.need_update == 'true'
        run: |
          rm -rf /tmp/FnDepot
          git clone "https://${{ secrets.FnDepot_PAT }}@github.com/moxyis/FnDepot.git" /tmp/FnDepot
          echo "✅ FnDepot仓库克隆完成"

      - name: 13. 复制fpk文件到FnDepot
        if: steps.compare.outputs.need_update == 'true'
        run: |
          mkdir -p /tmp/FnDepot/fpk-napcatqq
          cp "${{ steps.build_fpk.outputs.fpk_release_file }}" /tmp/FnDepot/fpk-napcatqq/
          echo "✅ fpk文件已复制到FnDepot"

      - name: 14. 更新FnDepot的fnpack.json
        if: steps.compare.outputs.need_update == 'true'
        run: |
          set -euo pipefail
          VERSION="${{ steps.remote_ver.outputs.remote_version }}"
          JSON_FILE="/tmp/FnDepot/fnpack.json"
          
          # 初始化JSON文件（若不存在）
          [ ! -f "$JSON_FILE" ] && echo '{}' > "$JSON_FILE"
          
          # 更新版本号（确保结构正确）
          jq --arg ver "$VERSION" \
            '.["fpk-napcatqq"] = (.["fpk-napcatqq"] // {}) | .["fpk-napcatqq"].version = $ver' \
            "$JSON_FILE" > "$JSON_FILE.tmp" && mv "$JSON_FILE.tmp" "$JSON_FILE"
          
          echo "✅ fnpack.json更新完成：$(cat "$JSON_FILE")"

      - name: 15. 更新FnDepot的README.md（修复匹配规则）
        if: steps.compare.outputs.need_update == 'true'
        run: |
          set -euo pipefail
          VERSION="${{ steps.remote_ver.outputs.remote_version }}"
          README_FILE="/tmp/FnDepot/README.md"
          
          # 初始化README（若不存在）
          if [ ! -f "$README_FILE" ]; then
            echo "# FnDepot" > "$README_FILE"
            echo "" >> "$README_FILE"
            echo "| 应用名称       | 版本号   | 来源仓库                                  | 类型   |" >> "$README_FILE"
            echo "|----------------|----------|-------------------------------------------|--------|" >> "$README_FILE"
            echo "| **NapCatQQ**   | $VERSION | NapNeko/https://github.com/NapNeko/NapCatQQ | Docker |" >> "$README_FILE"
            echo "" >> "$README_FILE"
            echo "自动更新说明：每日同步最新版本" >> "$README_FILE"
            echo "✅ 初始化README.md完成"
          else
            # 修复匹配规则：精确匹配NapCatQQ所在行的版本号（兼容任意空格和格式）
            sed -i -E \
              's/(^\|.*\*\*NapCatQQ\*\*.*\|[[:space:]]*)[0-9.]+([[:space:]]*\|.*$)/\1'"${VERSION}"'\2/' \
              "$README_FILE"
            echo "✅ README.md版本号已更新为：$VERSION"
          fi
          
          # 验证更新结果
          echo -e "\n📄 README.md关键内容："
          grep -A 1 "NapCatQQ" "$README_FILE"

      - name: 16. 提交并推送FnDepot变更
        if: steps.compare.outputs.need_update == 'true'
        run: |
          set -euo pipefail
          cd /tmp/FnDepot
          
          # 暂存变更
          git add fpk-napcatqq/ fnpack.json README.md
          
          # 提交
          COMMIT_MSG="chore: 更新napcatqq至v${{ steps.remote_ver.outputs.remote_version }}"
          git commit -m "$COMMIT_MSG" || echo "✅ FnDepot无变更"
          
          # 推送（带重试）
          git push origin main || (
            echo "⚠️ 推送失败，尝试拉取后重试" &&
            git pull --rebase origin main &&
            git push origin main
          )
          echo "✅ FnDepot仓库更新完成"

      # ===================== 第五步：创建GitHub Release =====================
      - name: 17. 创建Release并上传文件
        if: steps.compare.outputs.need_update == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ steps.remote_ver.outputs.remote_version }}-linux"
          name: "napcat-docker v${{ steps.remote_ver.outputs.remote_version }}"
          body: |
            ## 自动更新说明
            - 版本：v${{ steps.remote_ver.outputs.remote_version }}
            - 架构：Linux-AMD64
            - 哈希：${{ steps.remote_ver.outputs.remote_digest }}
            - 更新时间：${{ github.run_at }}
          files: ${{ steps.build_fpk.outputs.fpk_release_file }}
          overwrite: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ===================== 第六步：异常日志上传 =====================
      - name: 18. 上传调试日志（失败时）
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: debug-logs-${{ github.run_id }}
          path: |
            fpk-napcatqq/manifest
            docker-image-digest.txt
            /tmp/fpk_build/*
            /tmp/FnDepot/README.md
          retention-days: 7
