name: 版本比对后自动打包napcat-docker并更新仓库

on:
  workflow_dispatch:  # 手动触发
  schedule:
    - cron: '0 0 * * *'  # 每日UTC 0点（北京时间8点）执行

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 授予写入仓库/创建Release权限
      pull-requests: read
    steps:
      # ===================== 第一步：环境准备 =====================
      - name: 1. 检出当前仓库代码（带完整历史）
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 完整检出历史
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 2. 配置Git全局身份（跨仓库推送通用）
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: 3. 安装基础依赖（jq/curl/sed/skopeo）
        run: |
          sudo apt update && sudo apt install -y jq curl sed skopeo
          echo "基础依赖安装完成"

      # ===================== 第二步：版本比对（核心前置） =====================
      - name: 4. 读取本地manifest版本号（鲁棒版）
        id: local_ver
        run: |
          if [ -f "fpk-napcatqq/manifest" ]; then
            # 兼容version: / version=、空格、引号、v前缀
            LOCAL_VERSION=$(sed -n 's/^[[:space:]]*version[[:space:]]*[:=][[:space:]]*//p' fpk-napcatqq/manifest \
              | tr -d '"' | tr -d "'" | sed 's/^v//' | xargs)
            echo "本地manifest版本：$LOCAL_VERSION"
            echo "local_version=$LOCAL_VERSION" >> $GITHUB_OUTPUT
          else
            echo "❌ 未找到fpk-napcatqq/manifest，强制触发更新"
            echo "local_version=0.0.0" >> $GITHUB_OUTPUT
            mkdir -p fpk-napcatqq
            echo "version: 0.0.0" > fpk-napcatqq/manifest
            git add fpk-napcatqq/manifest
            git commit -m "chore: 初始化manifest文件" || echo "无需要提交的变更"
          fi

      - name: 5. 获取napcat-docker最新版本号（过滤latest）
        id: remote_ver
        run: |
          # 重试3次获取有效版本号
          for i in {1..3}; do
            REMOTE_VERSION=$(curl -sSL --retry 3 --connect-timeout 10 \
              "https://hub.docker.com/v2/repositories/mlikiowa/napcat-docker/tags?page_size=20&ordering=last_updated" \
              | jq -r '.results[].name' \
              | grep -v -E '^latest$|^dev$|^beta$' \
              | grep -E '^[0-9]+\.[0-9]+(\.[0-9]+)?$' \
              | head -n1)
            
            if [ -n "$REMOTE_VERSION" ] && [ "$REMOTE_VERSION" != "null" ]; then
              break
            fi
            echo "⚠️ 第${i}次获取版本号失败，重试中..."
            sleep 3
          done
          
          # 终极兜底
          if [ -z "$REMOTE_VERSION" ] || [ "$REMOTE_VERSION" = "null" ]; then
            echo "❌ 获取远程版本失败，使用兜底版本"
            REMOTE_VERSION="4.9.80"
          fi
          
          # 获取镜像哈希（用于版本比对）
          REMOTE_DIGEST=$(skopeo inspect docker://mlikiowa/napcat-docker:${REMOTE_VERSION} 2>/dev/null | jq -r '.Digest')
          if [ "$REMOTE_DIGEST" = "null" ] || [ -z "$REMOTE_DIGEST" ]; then
            REMOTE_DIGEST=""
          fi
          
          echo "远程最新版本：$REMOTE_VERSION"
          echo "远程镜像哈希：$REMOTE_DIGEST"
          echo "remote_version=$REMOTE_VERSION" >> $GITHUB_OUTPUT
          echo "remote_digest=$REMOTE_DIGEST" >> $GITHUB_OUTPUT
          echo "arch=Linux-AMD64" >> $GITHUB_OUTPUT  # 固定架构

      - name: 6. 读取本地哈希记录（避免版本号相同但镜像更新）
        id: local_digest
        run: |
          if [ -f "docker-image-digest.txt" ]; then
            LOCAL_DIGEST=$(cat docker-image-digest.txt | xargs)
          else
            LOCAL_DIGEST=""
            touch docker-image-digest.txt
          fi
          echo "local_digest=$LOCAL_DIGEST" >> $GITHUB_OUTPUT
          echo "本地镜像哈希：$LOCAL_DIGEST"

      - name: 7. 版本+哈希比对，判断是否更新
        id: compare
        run: |
          if [ "${{ steps.local_ver.outputs.local_version }}" = "${{ steps.remote_ver.outputs.remote_version }}" ] && [ "${{ steps.local_digest.outputs.local_digest }}" = "${{ steps.remote_ver.outputs.remote_digest }}" ]; then
            echo "✅ 本地版本与远程版本+哈希一致（${{ steps.remote_ver.outputs.remote_version }}），无需更新"
            echo "need_update=false" >> $GITHUB_OUTPUT
          else
            echo "🔄 版本/哈希不一致，开始更新打包流程"
            echo "need_update=true" >> $GITHUB_OUTPUT
          fi

      # ===================== 第三步：仅版本/哈希不一致时执行 =====================
      - name: 8. 更新manifest版本号 + 哈希记录
        if: steps.compare.outputs.need_update == 'true'
        run: |
          # 更新manifest版本号（兼容多种格式）
          sed -i -E \
            "s/(^[[:space:]]*version[[:space:]]*[:=][[:space:]]*)([\"']?)([0-9a-zA-Z.]+)([\"']?)/\1\2${{ steps.remote_ver.outputs.remote_version }}\4/" \
            fpk-napcatqq/manifest
          
          # 更新哈希记录
          echo "${{ steps.remote_ver.outputs.remote_digest }}" > docker-image-digest.txt
          
          echo "✅ manifest版本已更新为：${{ steps.remote_ver.outputs.remote_version }}"
          cat fpk-napcatqq/manifest | grep -i version
          echo "✅ 镜像哈希已更新为：${{ steps.remote_ver.outputs.remote_digest }}"
          cat docker-image-digest.txt

          # 暂存变更
          git add fpk-napcatqq/manifest docker-image-digest.txt

      - name: 9. 提交并推送manifest+哈希变更到当前仓库
        if: steps.compare.outputs.need_update == 'true'
        run: |
          git commit -m "chore: 更新napcat-docker版本至${{ steps.remote_ver.outputs.remote_version }}" || echo "✅ 无需要提交的变更"
          git push origin HEAD:${{ github.ref_name }}
          echo "✅ manifest+哈希已推送到当前仓库"

      - name: 10. 下载并安装fnpack（指定地址）
        if: steps.compare.outputs.need_update == 'true'
        run: |
          FNPACK_URL="https://static2.fnnas.com/fnpack/fnpack-1.0.4-linux-amd64"
          curl -SL --retry 3 --connect-timeout 10 -o /usr/local/bin/fnpack "$FNPACK_URL"
          chmod +x /usr/local/bin/fnpack
          
          if ! command -v fnpack &>/dev/null; then
            echo "❌ fnpack安装失败"
            exit 1
          fi
          echo "✅ fnpack安装完成：$(which fnpack)"

      - name: 11. 进入napcat-docker目录执行fnpack打包
        if: steps.compare.outputs.need_update == 'true'
        id: build_fpk
        run: |
          # 进入fpk-napcatqq目录
          cd fpk-napcatqq
          echo "🚀 进入fpk-napcatqq目录，执行打包命令：fnpack build"
          fnpack build
          
          # 查找打包后的fpk文件
          FPK_FILE=$(find . -name "*.fpk" -type f | head -n1)
          if [ -z "$FPK_FILE" ]; then
            echo "❌ 未找到打包后的fpk文件"
            exit 1
          fi
          
          # 拼接完整路径
          FPK_FULL_PATH=$(pwd)/$FPK_FILE
          # 固定名称（推FnDepot）
          FPK_FIXED_NAME="fpk-napcatqq.fpk"
          # 带版本号+架构名称（传Release）
          FPK_VERSION_NAME="fpk-napcatqq-v${{ steps.remote_ver.outputs.remote_version }}-${{ steps.remote_ver.outputs.arch }}.fpk"
          
          # 复制两份文件
          mv "$FPK_FULL_PATH" /tmp/$FPK_FIXED_NAME
          cp /tmp/$FPK_FIXED_NAME /tmp/$FPK_VERSION_NAME
          
          echo "✅ fnpack打包完成："
          echo "  - 固定名称：/tmp/$FPK_FIXED_NAME"
          echo "  - 版本+架构名称：/tmp/$FPK_VERSION_NAME"
          echo "fpk_file=/tmp/$FPK_FIXED_NAME" >> $GITHUB_OUTPUT
          echo "fpk_name=$FPK_FIXED_NAME" >> $GITHUB_OUTPUT
          echo "fpk_release_file=/tmp/$FPK_VERSION_NAME" >> $GITHUB_OUTPUT
          echo "fpk_release_name=$FPK_VERSION_NAME" >> $GITHUB_OUTPUT

      # ===================== 第四步：推送fpk到FnDepot仓库 + 更新配置文件 =====================
      - name: 12. 克隆FnDepot仓库
        if: steps.compare.outputs.need_update == 'true'
        run: |
          git clone https://${{ secrets.FnDepot_PAT }}@github.com/moxyis/FnDepot.git /tmp/FnDepot
          echo "✅ 成功克隆FnDepot仓库到/tmp/FnDepot"

      - name: 13. 复制fpk文件到FnDepot目标目录
        if: steps.compare.outputs.need_update == 'true'
        run: |
          # 创建目标文件夹
          mkdir -p /tmp/FnDepot/fpk-napcatqq
          
          # 删除旧文件
          rm -f /tmp/FnDepot/fpk-napcatqq/fpk-napcatqq.fpk
          
          # 复制新文件
          cp ${{ steps.build_fpk.outputs.fpk_file }} /tmp/FnDepot/fpk-napcatqq/
          
          echo "✅ fpk文件已复制到：/tmp/FnDepot/fpk-napcatqq/${{ steps.build_fpk.outputs.fpk_name }}"
          ls -lh /tmp/FnDepot/fpk-napcatqq/

      - name: 14. 更新FnDepot的fnpack.json中napcat-docker版本号
        if: steps.compare.outputs.need_update == 'true'
        run: |
          cd /tmp/FnDepot
          
          # 检查并创建fnpack.json
          if [ ! -f "fnpack.json" ]; then
            echo '{"fpk-napcatqq": {"version": ""}}' > fnpack.json
            echo "✅ 创建默认的fnpack.json文件"
          fi
          
          # 更新版本号
          jq --arg ver "${{ steps.remote_ver.outputs.remote_version }}" \
            '.["fpk-napcatqq"].version = $ver' fnpack.json > fnpack.tmp && mv fnpack.tmp fnpack.json
          
          echo "✅ fnpack.json更新完成，当前内容："
          cat fnpack.json

      - name: 15. 更新FnDepot的README.md中napcat-docker表格版本号
        if: steps.compare.outputs.need_update == 'true'
        run: |
          cd /tmp/FnDepot
          
          # 检查并创建README.md
          if [ ! -f "README.md" ]; then
            echo "# FnDepot\n\n| **NapCatQQ** | ${{ steps.remote_ver.outputs.remote_version }} | napcat-docker/https://hub.docker.com/r/mlikiowa/napcat-docker |\n\nnapcat-docker是通过Github-Actions进行每天自动化检测最新版本进行打包推送到本仓库\n脚本自动化仓库：https://github.com/moxyis/fpk-napcatqq" > README.md
            echo "✅ 创建默认的README.md文件"
          else
            # 精准更新表格版本号
            sed -i -E \
              "s/(\|\s*\*\*NapCatQQ\*\*\s*\|\s*)[0-9.]+\s*(\|\s*napcat-docker\/https:\/\/hub.docker.com\/r\/mlikiowa\/napcat-docker\s*\|)/\1${{ steps.remote_ver.outputs.remote_version }}\2/g" \
              README.md
              
            echo "✅ README.md中NapCatQQ表格版本已更新为：${{ steps.remote_ver.outputs.remote_version }}"
          fi
          
          # 输出关键内容
          echo "📄 更新后的README.md关键内容："
          grep -A 1 -B 1 "napcat-docker" README.md

      - name: 16. 提交并推送所有变更到FnDepot仓库
        if: steps.compare.outputs.need_update == 'true'
        run: |
          cd /tmp/FnDepot
          
          # 暂存变更
          git add fpk-napcatqq/${{ steps.build_fpk.outputs.fpk_name }} fnpack.json README.md
          
          # 提交变更
          git commit -m "chore: 更新napcat-docker版本至${{ steps.remote_ver.outputs.remote_version }}" || echo "✅ 无需要提交的变更"
          
          # 推送变更
          git push origin main
          echo "✅ 所有变更已推送到 https://github.com/moxyis/FnDepot/tree/main/fpk-napcatqq"

      # ===================== 第五步：上传FPK到当前仓库的GitHub Releases =====================
      - name: 17. 创建GitHub Release并上传FPK文件
        if: steps.compare.outputs.need_update == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "napcat-docker-v${{ steps.remote_ver.outputs.remote_version }}-${{ steps.remote_ver.outputs.arch }}"
          name: "napcat-docker v${{ steps.remote_ver.outputs.remote_version }}"
          body: |
            ## napcat-docker 自动打包更新
            - 版本：v${{ steps.remote_ver.outputs.remote_version }}
            - 平台架构：${{ steps.remote_ver.outputs.arch }}
            - 打包格式：fpk
            - 更新时间：${{ github.run_at }}
            - 打包工具：fnpack 1.0.4
            - 镜像哈希：${{ steps.remote_ver.outputs.remote_digest }}
            - FnDepot仓库：https://github.com/moxyis/FnDepot/tree/main/fpk-napcatqq
          prerelease: false
          files: ${{ steps.build_fpk.outputs.fpk_release_file }}
          overwrite: true  # 覆盖同名标签
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
